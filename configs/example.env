# Example Environment Configuration
# Copy this file to .env and customize for your setup

################################################################################
# REQUIRED: Site Configuration
################################################################################

# Site domains (use backticks, comma-separated)
# These domains MUST have DNS A records pointing to your server IP
SITES=`site1.example.com`,`site2.example.com`

# Email for Let's Encrypt SSL certificates
LETSENCRYPT_EMAIL=admin@example.com

################################################################################
# REQUIRED: Database Configuration
################################################################################

# MariaDB database password for frappe user
DB_PASSWORD=ChangeThisSecurePassword123

# MariaDB root password
MARIADB_ROOT_PASSWORD=ChangeThisRootPassword456

################################################################################
# REQUIRED: Image Configuration
################################################################################

# Frappe version to use
ERPNEXT_VERSION=version-15

# Custom image name and tag
CUSTOM_IMAGE=custom-frappe
CUSTOM_TAG=latest

# Don't pull from registry (we build locally)
PULL_POLICY=never

################################################################################
# OPTIONAL: Site Defaults
################################################################################

# Default administrator password (change after first login!)
ADMIN_PASSWORD=Admin@123

# Default apps to install on new sites
DEFAULT_APPS="erpnext hrms payments"

################################################################################
# OPTIONAL: Advanced Configuration
################################################################################

# Timezone
TZ=Asia/Kolkata

# Default site name (for bench CLI when no --site specified)
# FRAPPE_SITE_NAME_HEADER=site1.example.com

# GitHub Personal Access Token (for private repos)
# Generate at: https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_your_token_here

# Worker configuration
# WORKER_CLASS=gthread
# WORKERS=4
# THREADS=2

# Redis configuration
# REDIS_CACHE_MAX_MEMORY=1gb
# REDIS_QUEUE_MAX_MEMORY=1gb

# MariaDB configuration
# MARIADB_MAX_CONNECTIONS=500
# MARIADB_INNODB_BUFFER_POOL_SIZE=2G
